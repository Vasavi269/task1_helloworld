apiVersion: apps/v1
kind: Deployment
metadata:
  name: hello-world
  labels:
    app: "hello-world"

spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  selector:
    matchLabels:
      app: "hello-world"
  template:
    metadata:
      labels:
        app: "hello-world"
    spec:
      containers:
      - name: "hello-world"
        image: ghcr.io/username/package:latest
        ports:
          - containerPort: 8001
      imagePullSecrets:
        - name: github-container-registry
---
apiVersion: v1
kind: Service
metadata:
  name: "staging-service"
spec:
  selector:
    app: "uu-v2"
  type: LoadBalancer
  ports:
  - name: http
    port: 80
    targetPort: 8001
